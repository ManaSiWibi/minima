{{!< default}}
<div class="gh-page">
    <div class="gh-container">

        <div class="gh-postfeed">
        {{#foreach posts}}
        
        <ul>
            <li>
                
                <span class="gh-post-meta"><time >{{date format="YYYY MM DD"}}</time> -</span> <a class="gh-post-link" "{{url}}">{{title}}</a> 
    
                </li>
        </ul>

              {{!-- <a class="gh-card-link" href="{{url}}">
        <div class="gh-card-content">
            <span class="gh-card-meta"><time datetime="{{date format="YYYY-MM-DD"}}">{{date format="D MMM YYYY"}}</time> <span class="bull">&bull;</span> {{reading_time}}</span>
            <h2 class="gh-card-title">{{title}}</h2>
            <p>{{excerpt}}</p>
            <strong>Read more</strong>
        </div>
    </a> --}}
            {{!-- {{> "card" page=../pagination.page }}  --}}
            {{!-- partials/card.hbs --}}

        {{/foreach}}
        </div>

        {{!-- If JS isn't enabled, fallback to standard pagination --}}
        <noscript>
            <div class="gh-pagination">
                {{pagination}}
            </div>
        </noscript>
    </div>
</div>
